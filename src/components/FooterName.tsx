import React from "react";
import {
  Box,
} from "grommet";

type Props = unknown;

const FooterName: React.FC<Props> = (
  props,
) =>
{
  return (
    <Box
      pad={
        {
          vertical: "medium",
        }
      }
    >
      Stoxbie
    </Box>
  );
};

export default FooterName;
